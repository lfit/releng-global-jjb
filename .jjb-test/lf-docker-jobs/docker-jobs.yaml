---
- project:
    name: example-docker
    jobs:
      - '{project-name}-docker-jobs'

    project: 'docker/project'
    project-name: 'docker-project'
    branch: 'master'
    mvn-settings: 'docker-project-settings'
    dockerhub-release: true
