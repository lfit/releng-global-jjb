---
fixes:
  - |
    Add lf-infra-wrappers block to set jenkins-ssh-credential parameter to
    the value in configuration parameter jenkins-ssh-release-credential,
    which makes the PyPI release merge template parallel to the release-job
    merge template.  Both need privileges to push a tag from the Jenkins minion.
    Document the revised configuration parameter.
